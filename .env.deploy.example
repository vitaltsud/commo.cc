# Deploy target. Copy to .env.deploy and fill. Do not commit .env.deploy.
# Используются те же SSH-ключи, что и в проекте app.crmca.cc (доступ по ключам уже настроен).
# SSH target (user@host) — тот же хост, что и ai.crmca.cc
COMMO_SSH=root@84.247.164.76
# Path to app on server (where repo is cloned)
COMMO_REMOTE_PATH=/var/www/commo.cc
# Optional: path to SSH key (empty = default, как в crmca.cc)
# COMMO_SSH_KEY=
